		<meta n:foreach="$httpEquivs   as $httpEquiv => $content" http-equiv="{$httpEquiv}" content="{$content}">
		<meta n:foreach="$metas        as $name => $content" name="{$name}" content="{$content}">
		<meta n:foreach="$appLinks     as $name => $content" property="{$name}" content="{$content}">
		<meta n:foreach="$ogs          as $name => $content" property="{$name}" content="{$content}">
		<meta n:foreach="$fbMetas      as $name => $content" property="{$name}" content="{$content}">
		<meta n:foreach="$twitterMetas as $name => $content" name="{$name}" content="{$content}">
